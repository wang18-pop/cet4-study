# 四级备考网站 · 部署指南

这个网站是纯静态页面，不需要安装依赖，也不用构建。你可以把它部署到 GitHub Pages，生成一个任何人都能用手机浏览器打开的公开网址。

## 一、本地预览

Windows 上双击 `start.bat`，或在项目目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File serve.ps1
```

然后打开 http://127.0.0.1:8000/ 即可预览。

## 二、发布到 GitHub Pages

1. 在 GitHub 新建一个仓库，例如 `cet4-study`。
2. 把本项目全部文件推到 `main` 分支：

```bash
git init
git add .
git commit -m "四级备考网站"
git branch -M main
git remote add origin https://github.com/你的用户名/cet4-study.git
git push -u origin main
```

3. 仓库设置里依次打开 Settings → Pages，把 Source 选为 **GitHub Actions**。
4. 推送后，`.github/workflows/deploy-pages.yml` 会自动发布。完成后，Actions 页面会给出公开网址，形如：

```text
https://你的用户名.github.io/cet4-study/
```

## 三、媒体与离线

真题 PDF 和听力音频默认通过 jsDelivr CDN 加载（来源：`catteacher0515/cet4-download` 开源仓库）。首次打开需要联网。

如果想把这些媒体下载到本站本地、脱离 CDN 自托管，执行：

```powershell
powershell -ExecutionPolicy Bypass -File tools/download-media.ps1
```

它会下载到 `media/` 目录，并生成 `data/media-local.json`。之后页面会优先使用本地文件。

## 四、检查数据

发布前可运行校验脚本，确认 JSON 和字段完整：

```powershell
powershell -ExecutionPolicy Bypass -File tools/check.ps1
```

## 五、说明

- 真题题库的 PDF 与音频来自开源项目，本站不打包这些大文件。
- 真题官方答案不在该开源仓库中，因此真题套卷当前采用“自测”模式；原创题与语法/词汇小测均为本站自建数据，可自动判分。
- 所有学习进度、错题、统计都保存在浏览器本地（localStorage），无需登录。
